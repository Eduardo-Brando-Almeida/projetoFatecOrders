"use client";

import Layout from "@/components/templates/ui/organisms/Layout";
import { Box } from "@mui/material";

const Home = () => {
  return (
    <Layout name="layout">
      <Box> Bem vindo! </Box>
    </Layout>
  );
};

export default Home;
